describe('Jest Setup', () => {
  test('should initialize without errors', () => {
    expect(true).toBe(true);
  });

  test('should have basic arithmetic working', () => {
    expect(2 + 2).toBe(4);
  });
});
